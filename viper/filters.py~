from datetime import datetime
import string

def datetimeformat(value, format='%d-%m-%Y %H:%M'):
	if value != None and (type(value) is datetime or type(value) is str):
		return value.strftime(format)
	return ''	
